- endpoint: {{.PROMETHEUS_HOST}}
  profile: metrics.yml
  token: {{.TOKEN}}
- endpoint: {{.PROMETHEUS_ISTIO_HOST}}
  profile: istio-metrics.yml
  username: internal
  password: {{.PROMETHEUS_PASSWORD}}
